class Ranking {
  constructor({ university, scores, compositeScore }) {
    this.university = university;
    this.scores = scores;
    this.compositeScore = compositeScore;
  }
}

module.exports = Ranking;